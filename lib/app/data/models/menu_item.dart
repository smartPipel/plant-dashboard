import 'package:flutter/material.dart';

class MenuItem {
  final String menuName;
  final IconData iconData;
  final Color iconColor;
  final Widget className;

  MenuItem(this.menuName, this.iconData, this.iconColor, this.className);
}
